import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    largeText:{
        color:'black',
        fontSize: 25,
        fontWeight: '200',
        marginBottom: 30
    }
})

export default styles;