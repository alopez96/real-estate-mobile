import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    input: {
        height: 40,
        margin: 12,
        minWidth: 150,
        borderWidth: 1,
        borderRadius: 10,
        paddingLeft: 10
      },
});

export default styles;